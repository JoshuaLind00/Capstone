using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManager : MonoBehaviour
{
    /*private Animator anim;
    [SerializeField] private bool attkL = false;
    [SerializeField] private bool attkH = false;
    [SerializeField] private bool attkM = false;
    [SerializeField] private bool isGrounded;
    private float currentHealth, maxHealth, currentMagic, maxMagic, currentStamina, maxStamina;
    public float castingReq = 30;
    public float regen;

    void Awake()
    {
        anim = GetComponent<Animator>();
        currentHealth = 100;
        currentMagic = 100;
        currentStamina = 100;
        maxHealth = 100 * 1 + (1 + StatManager.StrengthStat * 0.1f);
        maxMagic = 100 * 1 + (1 + StatManager.MagicStat * 0.1f);
        maxStamina = 100 * 1 + (1 + StatManager.StaminaStat * 0.1f);

        //UpdateHealth();


    }   


    void Update()
    {

         

        
    }
    */

}
